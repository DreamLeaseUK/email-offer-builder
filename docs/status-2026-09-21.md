# DreamLease Offer Mailer — status and build log (session 5)

Last updated: 21 September 2026. **This is the current-state build log.** `status-2026-09-18.md` (session 4, the
brochure finder) and earlier are history. The technical map is `architecture.md`; the brochure design and its
evidence are in `brochure-finder-brief.md`; the four rules and the working method are in `CLAUDE.md`.

## 0. Read this first — the day had two halves
**Morning (§1–§6):** the brochure finder's European English-language fallback and its small print; merged to `main`,
pushed, deployed as **v0.5.0**. **Afternoon (§7):** Matt's first real test sends, which changed the email itself:
the scope narrowed to Gmail + New Outlook, the real send path turned out to rewrite the HTML, test sends moved onto
production storage (`pnpm dev:live`), and **every multi-offer email became one offer per row**. All of it is in
`main` on GitHub. **Late afternoon (§8):** three finder defects that made it miss brochures a rep finds in 30 seconds
(Renault 4, Geely EX2) were fixed: **finder-1.3**, commit `2d7d227`. **194 tests** (schema 18, render 37, adapters 80,
api 59). Production is still the v0.5.0 deploy: the afternoon's render changes need no deploy to be used (§7.3).

## 1. Headline
The brochure finder was too locked down (Matt: a Polestar 2 search attached nothing). It now has **two tiers**:
the **UK edition is still the target**; when nothing UK verifies, the fallback is the **manufacturer's own European
brochure, as long as it is in English**, and the email tells the recipient when that is what they are getting.
Built, tested (184 tests at the time; 189 by the end of the day, §0), proven live through `wrangler dev` and in
the interface. **Merged to `main` (fast-forward), pushed to GitHub, and deployed: production Worker is v0.5.0**
(Matt, 21 Sept; no D1 migration — the new fields live in JSON snapshots). After the deploy `/health` answers
`version 0.5.0, db ok, images true, firecrawl false`; `/api/*` still answers **503** (no Cloudflare Access yet), so the
interface is still tested at `http://localhost:5173`.

## 2. Why Polestar 2 found nothing (from the stored trace of Matt's search, 21 Sept 06:52)
Two separate causes, both fixed:
1. **No UK evidence** — the only current Polestar 2 document on polestar.com is the European fleet brochure
   (`…fleet_polestar-2_brochure_my27_eu_260402.pdf`); Polestar UK publishes none. The UK-only rules could never pass it.
2. **Dated wrongly** — the finder read "July 2024" from a footnote in the first four pages and called the file
   14 months old. The file name carries its real date (`260402`, and a CMS upload timestamp `1775572382` = 7 Apr 2026),
   which the finder did not read.

## 3. The rules as of the morning (finder-1.1; §8 takes them to finder-1.3)
- **Tier 1, UK — unchanged.** Same evidence, same order: UK PDF, then the UK web brochure.
- **Tier 2, European fallback — only when tier 1 attached nothing.** The document must be on the manufacturer's
  official host (or linked from its official pages), name the make and model, be a **brochure**, be inside the
  12-month age limit, **be in English** (function-word check against ten European languages; a bilingual file or a
  bare table of figures does not pass), and **say it is European** (its address — `_eu_`, `/eu/`, `/ie/`, `.ie`, a
  European locale — or a € price, or the words Europe / European / EU, or a link from the official pages).
- **Never used:** a European **price & spec guide** (not the UK's prices or trims — Audi Ireland and VW Ireland
  publish exactly these); the **rest of the world** (US, Canada, Australia, NZ, Asia, South Africa…), dropped on the
  address without being opened; a document that **does not say which market it is for**.
- **Dates:** the URL date reader now understands CMS file names — an upload timestamp in front, and a compact
  edition date (YYMMDD, YYYYMMDD, YYYYMM) in the name. The newest of text and URL still wins.
- **Ireland counts as official:** `volkswagen.ie`, `kia.ie` etc. pass the official-host check (the likeliest source
  of an English-language European brochure).
- **Discovery:** European documents the first searches turn up are kept for tier 2 instead of being thrown away
  as "another market". If none is worth opening, tier 2 runs **one** extra search (`<make> <model> brochure Ireland
  Europe`, PDFs only) and opens at most two documents. A failure in tier 2 never undoes the tier 1 result.
- **Cost:** a document read for tier 1 that failed on UK evidence alone is re-judged for tier 2 from the same
  reading (never paid for twice). Same 25-credit cap.
- **What comes back:** still one of the six outcomes. A fallback is `verified_pdf` / `verified_web_brochure` with
  **`market: 'eu'`**, the flag `european_edition` (plus `euro_pricing` when it shows € prices). The stored record
  carries `market: 'eu'`, is titled **"… brochure (European edition)"** (never "(UK)") and its note says no UK edition
  was verified. `Brochure.market` / `BrochureSearch.market` are new optional schema fields (JSON snapshots: no D1
  migration).
- **Old "nothing found" results are not remembered across a rules change:** a remembered negative made by an older
  finder version is searched again (this is why Polestar 2 re-ran without pressing "Search again").
- **The rep sees it:** the attached panel shows an orange "European edition" note (what it is, that specification,
  equipment and any prices are not the UK's, "replace it if you have the UK one"); the trace says why it was accepted.
- **The recipient is told (the disclaimer, Matt 21 Sept):** when the attached brochure has `market: 'eu'` the card's
  small print gains, after "Brochure figures are the manufacturer's and may differ from this offer.": **"This is the
  manufacturer's European brochure; specification, equipment and prices may differ from UK models."** grid3's one
  shared footnote gains "Where a brochure is the manufacturer's European edition, specification, equipment and prices
  may differ from UK models." Email, plain text and hosted page alike. It is data-side text in `render`'s viewmodel
  (`EUROPEAN_BROCHURE_NOTE`), like the sentence it follows: v5 markup and `MARKUP_VERSION` untouched. The link label
  is unchanged ("Download brochure (PDF)"). **Wording is Matt's; Emma has not approved it yet** (brief §5.8 step 9:
  she approves brochure small print with the template block).

## 4. Evidence
- **Live, through the real Worker (`wrangler dev`):** Polestar 2 → `verified_pdf`, market `eu`, the exact PDF Matt
  named, edition 2026-04-07, **11 credits, 6 s**, 30.3 MB stored in R2 and served from `/b/:id` as
  "Polestar 2 brochure (European edition).pdf"; the second lookup is `stored` (0 credits). The Australian MY25
  brochure on the same host was dropped as another market without being opened.
- **Live, a car with no brochure anywhere (VW ID.4):** nothing attached — correct. Tier 2 found and opened
  volkswagen.ie's official `ID4_MY25.pdf` (a product & price guide, April 2025) and rejected it; Irish dealers'
  copies were refused as not official. 16 credits.
- **In the interface (localhost:5173):** real Polestar 2 offer → Add brochure → the European-edition panel; Update
  preview → the card's small print carries the European-brochure sentence (rendered by the real Worker).
- **Replays (zero credits):** all 12 recorded manufacturer sites still give the same answers; the real Polestar 2
  run and Audi Ireland's two price guides (recorded 21 Sept) are new fixtures. Plus scripted cases: UK still wins
  and the European copy beside it is never opened; German refused; European price guide refused; unmarked-market
  document refused; € -priced one accepted and flagged; the record is titled and marked.
- `diff-reference`: the same 8 pre-existing lines (2 the logo width, 6 the third hero pill); render untouched that
  morning (22 after the afternoon, §7.3). Typecheck clean.
- Firecrawl credits used this session: 68 (4,021 of 5,000 left; period ends 9 Oct).

## 5. Decisions made in the build that Matt can overturn
- **Brochures only in tier 2** — a European price & spec guide is never attached.
- **A € -priced European brochure IS attached**, flagged `euro_pricing` and shown to the rep. (Alternative: refuse it.)
- **A document that does not say its market is refused** rather than passed off as European.
- **An attached European edition keeps the normal 90-day life** — a UK edition published in the meantime is picked
  up at the next refresh or when a rep presses "Search again".

## 6. Next
- **Emma to approve the European-brochure small print** (§3) along with the rest of the compliance wording.
- The `brochure-finder` branch is fully merged; it can be deleted on GitHub whenever Matt likes.
- Unchanged from session 4: badge control (fixed list vs free text — rule 3 says fixed list), BCH/salsac fee,
  step 8 (cross-client validation and equal-height columns were also on this list in the morning; both were
  overtaken that afternoon — §7.1, §7.3); owed by others — IT (Access + subdomain + Graph app),
  Emma (approved wording + retention period), Matt (prod Firecrawl secret: `/health` reports `firecrawl:false`).
- `wrangler` is signed in as Matt with deploy rights (checked 21 Sept; an earlier 7403 on a remote D1 call did not recur).

## 7. Afternoon: the first real test sends, and what they changed

### 7.1 Scope (Matt)
"Let's pair this back, I need something more agile. Let's just get it working on Gmail and Outlook new." The
full client matrix and classic Outlook are out of scope for now. The heavier assurance approach proposed that
morning (certify the markup on real clients per `MARKUP_VERSION` with a screenshot service, certify each send
path, automatic pre-send check in the tool) is recorded in `architecture.md` A5 and **not built**.

### 7.2 What the test sends showed
Path: Compose → Copy for Outlook → paste into a New Outlook message → send to Gmail and Outlook.com.
1. **Every image and link was dead, in every client.** The campaign was made on local storage (`pnpm dev`), but the
   email points its images, hosted page and tracked links at production, which held none of them: vehicle images,
   headshot, `/c/<slug>` and `/r/<slug>/<link>` all 404 (checked); only the logo loaded. Outlook hid it because it
   blocks images by default.
2. **The paste rewrites the email, for every recipient.** Survives: tables, widths / `max-width`,
   `display:inline-block`, backgrounds, borders, radius, bold, letter-spacing, link colours. Lost: the `<style>`
   block (media query, forced-light overrides), the conditional comments, and a float's clearing spacer.
   **Text colour and size arrive flattened** (28px red price and red make name → black, body-sized; white badge text
   → black; button text underlined). Cause not established: Outlook's "merge formatting" paste option is the
   leading suspect. **Open:** needs the as-received source (Gmail → ⋮ → Show original → Download) and which paste
   option Outlook used.
3. **Outlook mobile kept two columns** and the side-by-side cards were uneven; **Gmail mobile** stacked the cards
   but ran the make name beside the badge and broke it ("VOLKSWA / GEN").

### 7.3 What was built
- **`pnpm dev:live`** (`wrangler dev --remote`): the local tool on **production** D1 / R2 / Images, so a campaign made
  at `localhost:5173` is a real one. Proven: dev sign-in works, it reads the production database, a looked-up
  vehicle photo was stored in production R2 and loaded from the public URL with no login. Consequences: local
  sender details / headshot / brochures are not there (re-enter once; brochures re-search); test campaigns land in
  the production promotions register (empty before today; wipe before go-live). The local code renders and the
  hosted page is written at creation, so **render changes are live through `dev:live` without a deploy**.
- **Matched rows** (`match.ts`, `measure.ts`): side-by-side cards come out the same height — a card reserves the
  badge row, brochure row and extra text lines its row-mate has (empty cells + `min-height`). `measure.ts` estimates
  where Arial wraps without a browser: 15 of 15 strings agreed with Chrome. Measured in a real browser with
  deliberately mismatched cards: two-up rows 738 = 738 and 694 = 694, three-up 479 ×3 and 419 ×3, buttons level.
- **One offer per row** (Matt: "I don't think the dual column format makes any sense… easier to just stack all
  offers vertically… reduce the crowded feeling"): auto layout is 1 → single, 2+ → stack; the layout picker is
  removed from Compose. grid2 / grid3 still render for a stored campaign that names one; their code (and the
  matched rows) is now dormant and is to be **deleted once Matt confirms the stacked layout in both clients**.
- **Three departures from the v5 reference, for the paste path** (header of `cards.ts`; `diff-reference` now reports
  22 lines: 8 older ones — 2 the logo width, 6 the third hero pill — and 14 for the pills and the stack image column;
  the wrapper sits outside the sections it compares; **`MARKUP_VERSION` was not bumped, still 2**, so templates
  approved against it still render — whether Emma should re-approve the changed markup is undecided): a **fluid wrapper** (100% up to 600px, not fixed 600px — why Outlook
  mobile shrank the desktop layout instead of reflowing); **inline-block badge pills** instead of `align="left"`
  floats (the clearing spacer did not survive); a **calc()-fluid image column** in the stack card (250px beside the
  details on a desktop, the full card width on a phone; no `calc()` = the old fixed column).
- Checked in a real browser with the style block and conditional comments stripped, as the paste leaves them:
  at 375px no sideways scroll, image 277px wide (was 218), badge and make name each on their own line, with or
  without a badge; at desktop width the 600px email and the 250 + 300 columns are unchanged. **Matt's re-test:
  "these tests are 100% better."** Real Outlook mobile was verified by Matt's send, not from here.

### 7.4 Open after the afternoon
1. Flattened text colour / size on the paste path (§7.2 item 2) — blocked on the as-received source.
2. Small print reads before the offer on a phone (it sits under the image in the stack card); proposed: move it
   below the button. Awaiting Matt.
3. Delete the grid code once the stacked layout is confirmed (above).
4. Emma: the European-brochure small print, with the rest of the compliance wording.
5. Rendering assurance (`architecture.md` A5) — proposed, not built.

## 8. Late afternoon: "the brochure skill is missing easy to find brochures" (finder-1.3)
Matt searched a Renault 4 and a Geely EX2 in the live tool; both came back "nothing found" although he found each
brochure by hand in 30 seconds. The stored traces showed three defects, none of them a missing brochure:
1. **Found, ranked first, thrown away unopened (Renault 4).** To avoid paying to open irrelevant files the finder
   wanted the make beside a numeric model name ("renault4") in a file's title or address. On its own site a
   manufacturer leaves the make out: Renault files it as `R4-eBrochure.pdf` (Peugeot: `208-brochure.pdf`). Fix:
   `modelHint()`, a looser test used ONLY to decide what to open (the number standing alone, or behind the make's
   initial). The document's own text is still held to the strict matcher, so a wrong guess costs a read, never a
   wrong attachment; candidates that name the model outright are opened first.
2. **The right page was opened, the brochure on it was invisible (Geely EX2).** "Download Geely EX2 Brochure" is a
   BUTTON, not a link; the address sits only in the page's embedded data, slashes escaped. The finder read links
   from the markdown only. Fix: `pdfUrlsInHtml()` also reads PDF addresses from the page's raw HTML (same one
   credit), keeping those whose file name names the model and says what it is.
3. **Lost provenance.** A PDF the search had already turned up kept its first sighting when the official page
   linked it too, so an official brochure on a CDN host was refused as "not linked". The official sighting now
   upgrades the candidate.
Evidence, live through the tool on production storage: Renault 4 → UK brochure, edition 2026-07-01, 9 credits;
Geely EX2 → UK brochure from `geelyauto.co.uk`, edition 2026-07-01, 11 credits; both stored and served from `/b/:id`.
The earlier "nothing found" results were re-searched by themselves (`FINDER_VERSION` changed). Both real runs are
zero-credit replays now (15 recorded sites); the 13 earlier ones give the same answers.
**Matt's verdict: "It's not set up properly… must be resolved."** These surfaced only because he searched those two
cars. Proposed and awaiting his word: one sweep across every make DreamLease sells (~25 models, ~250–300 credits),
fix each failure class found, record every run as a permanent test.
**Security note found by the doc audit:** production has `ACCESS_AUD` EMPTY (Access not configured); `/api` is
closed there only because `DEV_USER_EMAIL` is not defined in production. Setting it as a production var would
open the whole API with no login. Recorded in `CLAUDE.md`.
