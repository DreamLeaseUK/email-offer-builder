# DreamLease Offer Mailer — status and build log

Last updated: 14 September 2026, end of day. Author: Claude Code with Matt Wilson.
Companion docs: `dreamlease-offer-mailer-brief.md` (v1.1, the solution design), `dreamlease-offer-mailer-email-template-v2-brief.md` (Claude Design brief for the template), `PICKUP-PROMPT.md` (how to resume).

---

## 1. Where the build is

| Step (brief §8.2) | State | Notes |
|---|---|---|
| 1. Scaffold, schemas, D1, Worker, Access, deploy | **Done, committed** (`7bc5777`) | Live at https://offer-mailer.matt-wilson-9b8.workers.dev/health |
| 2. `render()`, four layouts, hosted page, prove in real clients | **Built, deployed, committed** (`f2b6372`) | Rendering proven in Chrome, Outlook iOS, Gmail iOS. Classic Outlook and New Outlook desktop have open issues (§4); a three-part diagnostic went to Matt on 14 Sept (§5). |
| 3. URL lookup, image pipeline, brochure harvest | **Built, deployed, committed** (14 Sept; reverted and restored the same day) | `POST /api/offers/lookup` proven against the live site through `wrangler dev`; brochure routes proven with real D1/R2 in tests. Firecrawl key not yet set (§7). |
| 4. Web app | Not started | |
| 5. Graph draft, Copy for Outlook | Not started | |
| 6. Redirects, click logging, stats | Not started | Every email link 404s until this exists |
| 7. Template admin, approval, register, suppression | Not started | |
| 8. Stubs and `docs/evolution.md` | Not started | |

**Step 2 is committed** (`f2b6372`, 14 Sept). That afternoon Matt supplied the v5 markup reference (`design/dreamlease-offer-mailer-v5.html`) with implementation notes (`docs/offer-mailer-implementation-notes.md`); `packages/render` was re-translated to it, deployed, and the six fixture `.eml` files sent to Matt for the acceptance pass. The three-part card diagnostic sent earlier that day is obsolete; its script was removed. Step 3 was built the same afternoon, reverted at Matt's request (`d02e818`) and restored on his "continue" (`fa14c03` re-applied on top of the v5 render).

## 2. Decisions made since the brief was written

All dated 14 Sept 2026 unless noted.

- **Workers Paid plan** confirmed by Matt but not yet switched on in the dashboard (no card to hand). Free plan is fine until step 3.
- **No MJML.** The v4 Claude Design file is hand-built email HTML; `packages/render` reproduces it as TypeScript template functions. `MARKUP_VERSION` pins what Emma approved.
- **Layout is per campaign** (`Campaign.layout`, `auto | single | stack | grid2 | grid3`), not per template. Auto: 1 → single, 2 → grid2, 3 → stack, 4+ → grid2. grid3 only when chosen; the first review found it small.
- **Email width 640px**, fluid container `width:100%; max-width:640px`, with the standard `[if mso]` 640 ghost wrapper around the whole email. Widened from the design's 600 after the first Outlook review.
- **Badges are orange `#FF8811`** (Matt, 11 Sept), one row per card, capped at 3 / 2 / 2 / 1 for hero / stack / grid2 / grid3.
- **Brochure link under the button** on every card, not beside it. The two 14px brochure glyphs are deliberately deferred.
- **Rounded buttons and badges in classic Outlook use VML** in the exact construction proven by `packages/render/scripts/diag-vml.ts` variant V4. Do not add line-height inside them.
- **No `[if mso]` ghost tables inside the body.** Classic Outlook unwraps conditional comments when it sends or forwards, so ghost grids reached phones as two fixed columns squeezed to fit, and New Outlook's sanitiser leaves stray boxes at comment boundaries. Only the outer 640 wrapper remains.
- **Floated tables (`align="left"`) for the grid were tried and rejected** (`scripts/diag-grid.ts` G3). They fixed New Outlook desktop but Word does not float them, so classic Outlook showed a narrow card in a wide column.
- **Grid units are inline-block divs** with `width:100%; max-width:<cell>px`, wrapped to one column on phones by a media query and by natural wrapping. Card rows are given equal heights via fixed-height cells (badges, derivative, spec line, small print) because New Outlook strips `vertical-align`.
- **Word rules learned** (superseded that afternoon, see below): no `mso-padding-alt:0`, no `text-transform`, no inline-block tables, borders on cells not tables, spacing as cell padding or white cell borders (never margins or spacer cells), eyebrows and compliance title upper-cased in code.

Step 3 (14 Sept, afternoon):

- **The offer page is not fully server-rendered.** Identity, the four slugs, headline stats, quick-spec, processing fees and the default configuration are in the HTML (`window.motorleaseInit` plus a few elements). Prices, initial payment and the badge flags are loaded by the page's Vue component from `GET /api/carresults/GetOfferDropdownsForCar?...` (same origin, no auth). The `url` adapter therefore fetches the page (HTMLRewriter for identity and the image URL) and then that JSON endpoint for the configuration's price, options and flags. The JSON-LD `lowPrice` is kept only as a cross-check (the site's JSON-LD has an unbalanced brace, so it is read with a regex).
- **Initial payment is derived** the way the site does it: initial months × monthly (plus maintenance when included). The endpoint's `initialPayment` field is 0.
- **Term, mileage and initial-payment chips come from the endpoint's option lists**, not a hard-coded set; they are returned alongside the offer and cached with it.
- **CAP ID surfaces in two places**: the image URL (as before) and the endpoint's `rateBookIdentifier` (`cars-<capId>-p-12-48-6000-n`). The parser maps named fields only; tests assert the fixture's CAP ID digits never reach an Offer, the cache row or the API response. Test fixtures are the real pages with the image host replaced and the CAP ID zeroed.
- **Badges from the site**: `specialOffer` → hot badge "Special offer"; `isInStock` → "In stock"; the site's free-text tags map onto the fixed list by alias (e.g. "Limited Stock" → "Limited numbers"); anything not on the list is dropped. `stock` is `in_stock` / `limited` / unset.
- **Images**: Cloudflare Images binding (`TRANSFORM`) → 1200px-wide JPEG at quality 82 on white, stored as `vehicles/<sha256>.jpg`, served at `/f/vehicles/<sha>.jpg` with immutable caching. The site's images are 750px wide, so the stored copy is 750×500 (Images does not upscale); sharp at card size, slightly soft on a retina hero. A failed image is a warning, not a failed lookup.
- **Cache**: D1 `lookup_cache`, keyed by the canonical URL (contract type, slug, explicit configuration; tracking parameters dropped), 24 hours, holding the parsed result (offer + options), never HTML.
- **Brochures**: `POST /api/brochures/ensure` (stored → harvest → 404 for upload/paste), `POST /api/brochures/manual` (URL or multipart PDF), `GET /api/brochures/current`, public `GET /b/<id>` (serves our PDF inline, or redirects to the manufacturer's request page), `GET /f/brochures/<sha>.pdf`. Superseded copies keep serving. Harvest is search → allowlisted page scrape → manufacturer-site map, 15-credit cap, UK check by domain upgraded to content when the first two pages show £ or OTR and no €.
- **Tests**: `packages/adapters` under plain vitest with the wasm HTMLRewriter; `apps/api` under `@cloudflare/vitest-pool-workers` (vitest 4 in that package only) with real local D1, R2 and Images bindings. The pool's workerd trails wrangler's by a fortnight, so `vitest.config.ts` pins the test compatibility date; bump it with the wrangler one.
- **`pnpm deploy` is pnpm's own command**; the script is `pnpm run deploy`.

v5 reference adopted (14 Sept, afternoon; Matt's decision, "read and deploy the latest versions of the templates that have been optimised for various email clients"):

- **The reference markup wins over the day's client observations.** Ghost tables are back inside the body (one ghost `<td>` per card in rows of two or three, plus inside the stack card and the hero CTA row); buttons are td padding + `display:block` anchor + `mso-padding-alt:0`, no VML, square corners in Outlook classic accepted; width is 600 again; badge pills are fixed-width one-cell tables (126 hero, 100 stack/grid2, full-width single pill in grid3); images carry the reference's exact sizes; spacers are cells; forced light mode via the `color-scheme` metas, `[data-ogsc]`/`[data-ogsb]` overrides and `lock-*` classes. The observations that led to the earlier constructions are now §4 item 2, to be re-verified on this build.
- **Kept from the code's own rules where they do not conflict:** eyebrows, stat labels and the compliance title are upper-cased in code as well as by `text-transform`, so Word shows them upper-case too; every http link still goes through `/r/`; compliance and footer copy come from the template record.
- **Badges:** two per card (hot first) for hero, stack and grid2, one for grid3, per the reference's `badge1`/`badge2`.
- **Sender.headshotUrl** added (optional); the signature shows the 56px headshot only when set. Department senders have none.
- **Brochure glyphs** are 14px images on our origin, as the reference specifies; placeholders drawn in code until the design-system artwork arrives. `MARKUP_VERSION` bumped to 2.
- **Fidelity check:** `scripts/diff-reference.ts` diffs the rendered structure against the reference; the old `diag-card.ts` was removed.

## 3. Client verification matrix (v5 build, 14 Sept afternoon)

The v5 build has not been through a client pass yet. What is verified: `pnpm test` (51 tests, including the reference's non-negotiables), the fidelity diff against the reference (every section structurally identical apart from the two knowing differences in §2), and Chrome at 900px and 375px for all four layouts (two-up / three-up on desktop, one full-width column on the phone width, pills side by side, buttons and stat tiles as designed).

The earlier matrix (14 Sept, evening) recorded the pre-v5 build and is superseded; its observations are listed in §4 as things to re-check.

## 4. Open issues, in priority order

1. **Acceptance pass of the v5 build** (implementation notes, "Acceptance test"): classic Outlook, New Outlook wide and narrow pane, Outlook iOS and Gmail iOS via a New Outlook forward, then Outlook mobile dark mode, Gmail web, Apple Mail. Check specifically: grids keep their columns in classic Outlook with no empty trailing column; buttons are full-height pills; a 30-character CTA label wraps; grid3 at 375px; the classic Outlook → Gmail forward (use case 2).
2. **Three things the earlier review saw that the v5 constructions reintroduce**, to be confirmed or refuted on the v5 build: classic Outlook unwrapping `[if mso]` ghost tables on forward so phones receive fixed columns; Word not floating `align="left"` tables (the badge pills would drop below each other); Word behaviour with `mso-padding-alt:0` buttons. If any reproduces, it is a change to the reference, not a one-off patch (§5).
3. **Width is 600px again**, as the reference specifies. Matt had asked for 640 after the first review; keep or change is his call, and a change means changing the reference.
4. **Brochure glyphs and the headshot are placeholders drawn in code** (`/a/icon-doc-2x.png`, `/a/icon-external-2x.png`, `/a/headshot-placeholder.png`, 28px and 112px); replace with the design-system artwork when supplied. The rep headshot is the new optional `Sender.headshotUrl`; department senders have none.
5. Hosted page is still the email markup in a page wrapper; the design-system hosted page from brief §7.1, the tool screens and the one-page guide are still owed by Claude Design.

## 5. How to work on the template from here

The markup is no longer the code's own invention: `design/dreamlease-offer-mailer-v5.html` is the source of truth and `packages/render` reproduces it. Rules:

1. After any change to `cards.ts` or `render.ts`, run `pnpm --filter @offer-mailer/render exec tsx scripts/diff-reference.ts`. It renders each layout, strips data (text, href, src, alt), and diffs the tag-and-attribute structure of every section against the reference. Any difference that is not data is a deviation and must be justified in the header of `cards.ts`. Current knowing deviations: logo 98px wide (the asset is 196px, 99 would distort it); the salary-sacrifice price blocks and the red "View this offer" link, which the reference does not show but its brief requires.
2. Do not patch a construction in response to one screenshot. If a client shows a fault, build a labelled diagnostic `.eml` with the real card in each candidate construction (`scripts/diag-vml.ts` and `scripts/diag-grid.ts` show the pattern), have Matt check it in classic Outlook, New Outlook desktop (wide and narrow pane) and on his phone via a New Outlook forward, collect all results, then change the reference and the code together.
3. Regenerate the fixtures (`pnpm --filter @offer-mailer/render fixtures`), deploy (`pnpm run deploy`), and send Matt the `.eml` files from `packages/render/out/` for a full pass.

Test files are produced by `packages/render/scripts/write-fixtures.ts` into `packages/render/out/` (git-ignored). `.eml` files carry `X-Unsent: 1` so classic Outlook opens them as sendable drafts.

History, for context only: eleven builds went out on 14 Sept before the v5 reference arrived, each reacting to one screenshot; the constructions they settled on (no body ghost tables, VML pills, 640px, min-width pills) are superseded by the reference and the diagnostic that was testing them was withdrawn.

## 6. Things that exist and where

- **Live Worker:** https://offer-mailer.matt-wilson-9b8.workers.dev — `/health` (reports db, images, firecrawl), `/c/<slug>` hosted pages from R2, `/a/*` static assets (logo, vehicle placeholder), `/f/vehicles/<sha>.jpg` and `/f/brochures/<sha>.pdf` stored files, `/b/<id>` brochure links, `/api/offers/lookup`, `/api/brochures/*` and `/api/dev/preview` (behind Access; locally with `DEV_USER_EMAIL`). `/r` is a stub (step 6). Until Access is configured the `/api` routes answer 503 in production; test them with `wrangler dev` (`.dev.vars` supplies the dev user).
- **Hosted fixture page:** https://offer-mailer.matt-wilson-9b8.workers.dev/c/k3J9xQ2mZp8LwN4vR7tY (whatever fixture was last uploaded with `wrangler r2 object put`).
- **Cloudflare:** account, D1 id, R2 buckets in CLAUDE.md. `wrangler login` as Matt; the token expires, re-login from Matt's terminal with `& "C:\Users\MatthewWilson\email-offer-builder\apps\api\node_modules\.bin\wrangler.cmd" login`.
- **Markup source of truth:** `design/dreamlease-offer-mailer-v5.html` (send-ready reference, all four layouts) with `docs/offer-mailer-implementation-notes.md`; visual preview `design/offer-mailer-email-template-v2.dc.html`. The older `offer-mailer-email-template-v4.dc.html` is kept for history. `/a/` also serves `icon-doc-2x.png`, `icon-external-2x.png` and `headshot-placeholder.png` (placeholders drawn in code, see §4).
- **Config Matt maintains:** `config/badges.json`, `config/manufacturer-uk-domains.json`.
- **Tests:** 51 across schema, render and api; `pnpm test`, `pnpm typecheck` both clean at time of writing. `MARKUP_VERSION` is 2 (the v5 generation); the fixture template pins it.

## 7. Still owed by others

- IT: two Entra app registrations (Cloudflare Access IdP, Graph) and Access setup for `mailer.dreamlease.co.uk`; custom domains `mailer.` and `offers.dreamlease.co.uk`.
- Emma: approved compliance wording (the fixture text is a starting point, not approved).
- Whoever owns the offer feed: access, to decide whether the `feed` adapter replaces the scraper.
- Matt: Workers Paid plan when the card is available; Claude Design deliverables listed in §4.5; the Firecrawl key for the Worker (`cd apps/api && npx wrangler secret put FIRECRAWL_API_KEY`), without which brochure harvest answers 503 and the manual upload/paste path is the only one (the URL lookup does not need it).
